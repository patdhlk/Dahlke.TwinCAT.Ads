namespace Dahlke.TwinCAT.Ads.Alarms.Tests;

/// <summary>
/// Proves the test project is discovered and executed. Without this, an empty
/// suite reports success identically to a suite that never ran.
/// </summary>
public class ScaffoldingTests
{
    [Fact]
    public void TestHarness_Runs()
    {
        Assert.True(true);
    }
}
